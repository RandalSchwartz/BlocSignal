name: benchmarks
resolution: workspace
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  benchmark_harness: ^2.2.2
  bloc: ^8.1.4
  bloc_signals: ^1.0.0
  provider: ^6.1.2
  riverpod: ">=2.5.0 <4.0.0"
  signals_core: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.6
  very_good_analysis: ^11.0.0-rc.1
