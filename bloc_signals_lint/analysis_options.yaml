include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - example/**
  errors:
    # analyzer 8.0.0 marks declaredFragment / declaredElement as experimental
    experimental_member_use: ignore
